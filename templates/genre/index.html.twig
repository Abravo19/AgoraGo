{% extends 'base.html.twig' %}

{% block body %}
    <!-- page start--><div class="col-sm-8">
<section class="panel">
<div class="chat-room-head">
<h3><i class="fa fa-angle-right"></i> Gérer les genres</h3>
</div>
<div class="panel-body">

    <table class="table" table-striped table-advance table-hover"> 
        <thead>
            <tr>
                <th>class="tableau-entete"</th>
                <th><i class="fa fa-bullhorn"></i> Identif</th>
                <th><i class="fa fa-bookmark"></i> Libellé</th>
            </tr>
        </thead>
        <tbody>
        {% for genre in genres %}
            <tr>
                <td>{{ genre.id }}</td>
                <td>{{ genre.libGenre }}</td>
                <td>
                    <a href="{{path('app_genre_show',{'id': genre.id})}}"><button
class="btn btn-primary btn-xs"><i class="fa fa-tasks"></i></button></a>
<a href="{{path('app_genre_edit',{'id': genre.id})}}"><button
class="btn btn-primary btn-xs"><i class="fa fa-pencil"></i></button></a>
</td>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">aucun genre enregistré en base de données</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_genre_new') }}"><button class="btn btn-primary btn-
xs"><i class="fa fa-plus"></i></button></a>
</div><!-- fin div panel-body-->
</section><!-- fin section tournois-->
</div><!--fin div col-sm-8-->
{% endblock %}
